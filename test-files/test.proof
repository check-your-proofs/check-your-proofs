theorem test {
  conclude (\forall x\forall y(x\in y\to x\in y\wedge x\in y))\wedge(\forall x\forall y(x\in y\to x\in y\wedge x\in y)) {
    any x, y {
      assume x\in y conclude x\in y\wedge x\in y {}
    }
  }
}
